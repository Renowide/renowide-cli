{
  "name": "renowide-cli-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Monorepo for @renowide/cli, @renowide/types, @renowide/ui-kit.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/types && npm run build --workspace=packages/ui-kit && npm run build --workspace=packages/cli",
    "typecheck": "npm run typecheck --workspace=packages/types && npm run typecheck --workspace=packages/ui-kit && npm run typecheck --workspace=packages/cli",
    "test": "npm run test --workspaces --if-present",
    "clean": "rm -rf packages/*/dist packages/*/node_modules node_modules"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Renowide/renowide-cli.git"
  },
  "license": "MIT"
}
