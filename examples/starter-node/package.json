{
  "name": "renowide-agent-starter-node",
  "version": "0.2.0",
  "private": true,
  "description": "Node + TypeScript starter for a Renowide-published agent. Reference only — not published to npm.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@renowide/agent-sdk": "^0.4.0",
    "dotenv": "^16.4.5",
    "yaml": "^2.5.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
